---
title: "MetricType | Node.js"
slug: /node/node/Management-MetricType
sidebar_label: "MetricType"
beta: false
added_since: v2.3.x
last_modified: v2.6.x
deprecate_since: false
notebook: false
description: "这是一个枚举，提供以下常量。| Node.js"
type: docx
token: KQDEdcccXozuKpxDg78cSmxYnQg
sidebar_position: 21
keywords: 
  - Faiss
  - 视频搜索
  - AI 幻觉
  - AI Agent
  - zilliz
  - zilliz cloud
  - cloud
  - MetricType
  - nodejs30
displayed_sidebar: nodeSidebar

displayed_sidbar: nodeSidebar
---

import Admonition from '@theme/Admonition';


# MetricType

这是一个枚举，提供以下常量。

- **L2**

    将度量类型设置为欧几里得距离 (L2)。这仅适用于浮点向量。

- **IP**

    将度量类型设置为内积 (IP)。这仅适用于浮点向量。

- **COSINE**

    将度量类型设置为 COSINE。这仅适用于浮点向量。

- **HAMMING**

    将度量类型设置为 HAMMING。这仅适用于二进制向量。

- **JACCARD**

    将度量类型设置为 JACCARD。这仅适用于二进制向量。

- **BM25**

    将度量类型设置为 BM25。这仅适用于稀疏向量。

- **MHJACCARD**

    将度量类型设置为 MHJACCARD。这仅适用于二进制向量。

- **MAX_SIM**

    将度量类型设置为 MAX_SIM。这仅适用于嵌入列表。

- **MAX_SIM_COSINE**

    将度量类型设置为 MAX_SIM_COSINE。这仅适用于嵌入列表。

- **MAX_SIM_IP**

    将度量类型设置为 MAX_SIM_IP。这仅适用于嵌入列表。

- **MAX_SIM_L2**

    将度量类型设置为 MAX_SIM_L2。这仅适用于嵌入列表。

- **DTW_COSINE**

    将度量类型设置为 DTW_COSINE。这仅适用于嵌入列表。

- **DTW_IP**

    将度量类型设置为 DTW_IP。这仅适用于嵌入列表。
