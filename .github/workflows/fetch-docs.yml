name: fetch CN docs
on:
  workflow_dispatch:
    inputs:
      group:
        description: Content group to publish
        type: choice
        options: [all, guides, python, java, node, go, cli, rest]
        default: all
      artifact_retention_days:
        description: Artifact retention in days
        type: number
        default: 3
      target_branch:
        description: Existing branch to publish to
        type: string
        default: dev
      publish:
        description: Publish and translate produced artifacts
        type: boolean
        default: false
      tooling_ref:
        description: Non-production tooling ref override
        type: string
        default: master

permissions:
  contents: write
  actions: write

concurrency:
  group: cn-docs-production-dev
  cancel-in-progress: false

jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      master_sha: ${{ steps.refs.outputs.master_sha }}
      dev_baseline_sha: ${{ steps.refs.outputs.dev_baseline_sha }}
      target_branch: ${{ steps.refs.outputs.target_branch }}
      publish: ${{ steps.refs.outputs.publish }}
      selected_group: ${{ steps.refs.outputs.selected_group }}
      retention_days: ${{ steps.refs.outputs.retention_days }}
      card_id: ${{ steps.card.outputs.card_id }}
      card_started_at: ${{ steps.card.outputs.card_started_at }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0
      - id: refs
        name: Resolve immutable refs and selection
        env:
          SELECTED_GROUP: ${{ github.event.inputs.group || 'all' }}
          RETENTION_DAYS: ${{ github.event.inputs.artifact_retention_days || '3' }}
          TARGET_BRANCH: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.target_branch || 'dev' }}
          PUBLISH: ${{ github.event_name != 'workflow_dispatch' || github.event.inputs.publish == 'true' }}
          TOOLING_REF: ${{ github.event.inputs.tooling_ref || 'master' }}
        run: |
          set -euo pipefail
          if [[ "$TARGET_BRANCH" == -* || "$TARGET_BRANCH" == *:* || "$TARGET_BRANCH" == *$'\n'* || "$TARGET_BRANCH" == *$'\r'* ]]; then
            echo 'target_branch contains forbidden characters' >&2
            exit 2
          fi
          git check-ref-format --branch "$TARGET_BRANCH"
          tooling_ref="$TOOLING_REF"
          if [[ "$PUBLISH" == true && "$TARGET_BRANCH" == dev ]]; then
            tooling_ref=master
          fi
          if [[ "$tooling_ref" =~ ^[0-9a-f]{40}$ ]]; then
            git fetch --no-tags origin "$tooling_ref"
            master_sha=$(git rev-parse FETCH_HEAD)
          else
            if [[ "$tooling_ref" == -* || "$tooling_ref" == *:* || "$tooling_ref" == *$'\n'* || "$tooling_ref" == *$'\r'* ]]; then
              echo 'tooling_ref contains forbidden characters' >&2
              exit 2
            fi
            git check-ref-format --branch "$tooling_ref"
            git fetch --no-tags origin "refs/heads/$tooling_ref:refs/remotes/origin/$tooling_ref"
            master_sha=$(git rev-parse "refs/remotes/origin/$tooling_ref")
          fi
          git cat-file -e "$master_sha^{commit}"
          git fetch --no-tags origin "refs/heads/$TARGET_BRANCH:refs/remotes/origin/$TARGET_BRANCH"
          dev_baseline_sha=$(git rev-parse "refs/remotes/origin/$TARGET_BRANCH")
          [[ "$master_sha" =~ ^[0-9a-f]{40}$ && "$dev_baseline_sha" =~ ^[0-9a-f]{40}$ ]]
          case "$SELECTED_GROUP" in all|guides|python|java|node|go|cli|rest) ;; *) exit 2 ;; esac
          [[ "$RETENTION_DAYS" =~ ^[1-9][0-9]*$ ]]
          git checkout --detach "$master_sha"
          groups=(guides python java node go cli rest)
          [[ "$SELECTED_GROUP" != all ]] && groups=("$SELECTED_GROUP")
          card_parts=()
          if [[ "$SELECTED_GROUP" == all ]]; then
            card_parts=("Produce manuals (0/${#groups[@]})")
            [[ "$PUBLISH" == true ]] && card_parts+=("Publish sources (0/${#groups[@]})" "Translate manuals (0/${#groups[@]})" "Publish translations (0/${#groups[@]})" Verify)
          else
            for group in "${groups[@]}"; do
              card_parts+=("Produce $group")
              if [[ "$PUBLISH" == true ]]; then
                card_parts+=("Publish $group" "Translate $group" "Publish $group translation")
              fi
            done
            [[ "$PUBLISH" == true ]] && card_parts+=(Verify)
          fi
          card_stages=$(IFS=,; echo "${card_parts[*]}")
          printf 'master_sha=%s\ndev_baseline_sha=%s\ntarget_branch=%s\npublish=%s\nselected_group=%s\nretention_days=%s\ncard_stages=%s\n' "$master_sha" "$dev_baseline_sha" "$TARGET_BRANCH" "$PUBLISH" "$SELECTED_GROUP" "$RETENTION_DAYS" "$card_stages" >> "$GITHUB_OUTPUT"
      - uses: pnpm/action-setup@v4
        with: { version: 9 }
      - uses: actions/setup-node@v4
        with: { node-version: '20', cache: npm }
      - run: npm ci
      - id: card
        name: Create progress card
        run: npx docusaurus report-to-lark --card-create --title "$CARD_TITLE" --stages "$CARD_STAGES" --target-branch "$TARGET_BRANCH"
        env:
          CARD_STAGES: ${{ steps.refs.outputs.card_stages }}
          CARD_TITLE: ${{ steps.refs.outputs.publish == 'true' && 'CN Docs Build' || 'CN Docs Artifact-Only Build' }}
          TARGET_BRANCH: ${{ steps.refs.outputs.target_branch }}
          APP_ID: ${{ secrets.APP_ID }}
          APP_SECRET: ${{ secrets.APP_SECRET }}
          FEISHU_HOST: ${{ vars.FEISHU_HOST }}

  monitor_docs_progress:
    needs: [prepare]
    uses: ./.github/workflows/_monitor-docs-progress.yml
    with:
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      card_id: ${{ needs.prepare.outputs.card_id }}
      card_started_at: ${{ needs.prepare.outputs.card_started_at }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      selected_group: ${{ needs.prepare.outputs.selected_group }}
      publish_enabled: ${{ needs.prepare.outputs.publish == 'true' }}
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_SECRET: ${{ secrets.APP_SECRET }}

  produce_guides_sources:
    needs: prepare
    if: ${{ needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'guides' }}
    uses: ./.github/workflows/_fetch-guides-sources.yml
    with:
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      dev_baseline_sha: ${{ needs.prepare.outputs.dev_baseline_sha }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_SECRET: ${{ secrets.APP_SECRET }}
      SPACE_ID: ${{ secrets.SPACE_ID }}
      FIGMA_API_KEY: ${{ secrets.FIGMA_API_KEY }}
      MODEL_API_KEY: ${{ secrets.MODEL_API_KEY }}
      OSS_ACCESS_KEY_ID: ${{ secrets.OSS_ACCESS_KEY_ID }}
      OSS_ACCESS_KEY_SECRET: ${{ secrets.OSS_ACCESS_KEY_SECRET }}

  render_guides_tables:
    name: render_guides_tables / ${{ matrix.target_name }} / ${{ matrix.table_name }}
    needs: [prepare, produce_guides_sources]
    if: ${{ needs.produce_guides_sources.outputs.status == 'artifact_ready' && needs.produce_guides_sources.outputs.table_count != '0' }}
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix: ${{ fromJSON(needs.produce_guides_sources.outputs.table_matrix) }}
    uses: ./.github/workflows/_render-guides-table.yml
    with:
      table_id: ${{ matrix.table_id }}
      table_name: ${{ matrix.table_name }}
      table_slug: ${{ matrix.table_slug }}
      target: ${{ matrix.target }}
      target_name: ${{ matrix.target_name }}
      cleanup: ${{ matrix.cleanup }}
      source_artifact_name: ${{ needs.produce_guides_sources.outputs.artifact_name }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      dev_baseline_sha: ${{ needs.prepare.outputs.dev_baseline_sha }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}

  produce_guides:
    needs: [prepare, produce_guides_sources, render_guides_tables]
    if: ${{ always() && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'guides') && needs.produce_guides_sources.outputs.status == 'artifact_ready' && (needs.render_guides_tables.result == 'success' || needs.render_guides_tables.result == 'skipped') }}
    uses: ./.github/workflows/_assemble-guides.yml
    with:
      source_artifact_name: ${{ needs.produce_guides_sources.outputs.artifact_name || 'unavailable' }}
      table_matrix: ${{ needs.produce_guides_sources.outputs.table_matrix || '{"include":[]}' }}
      table_count: ${{ needs.produce_guides_sources.outputs.table_count || '0' }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      dev_baseline_sha: ${{ needs.prepare.outputs.dev_baseline_sha }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      cache_version: ${{ needs.produce_guides_sources.outputs.cache_version }}
      cache_save_required: ${{ needs.produce_guides_sources.outputs.cache_save_required }}
      assembly_decision_sha256: ${{ needs.produce_guides_sources.outputs.assembly_decision_sha256 }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
  produce_python:
    needs: prepare
    if: ${{ needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'python' }}
    uses: ./.github/workflows/_fetch-content-group.yml
    with:
      group: python
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      dev_baseline_sha: ${{ needs.prepare.outputs.dev_baseline_sha }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_SECRET: ${{ secrets.APP_SECRET }}
      SPACE_ID: ${{ secrets.SPACE_ID }}
      FIGMA_API_KEY: ${{ secrets.FIGMA_API_KEY }}
      MODEL_API_KEY: ${{ secrets.MODEL_API_KEY }}
      OSS_ACCESS_KEY_ID: ${{ secrets.OSS_ACCESS_KEY_ID }}
      OSS_ACCESS_KEY_SECRET: ${{ secrets.OSS_ACCESS_KEY_SECRET }}

  produce_java:
    needs: prepare
    if: ${{ needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'java' }}
    uses: ./.github/workflows/_fetch-content-group.yml
    with:
      group: java
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      dev_baseline_sha: ${{ needs.prepare.outputs.dev_baseline_sha }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_SECRET: ${{ secrets.APP_SECRET }}
      SPACE_ID: ${{ secrets.SPACE_ID }}
      FIGMA_API_KEY: ${{ secrets.FIGMA_API_KEY }}
      MODEL_API_KEY: ${{ secrets.MODEL_API_KEY }}
      OSS_ACCESS_KEY_ID: ${{ secrets.OSS_ACCESS_KEY_ID }}
      OSS_ACCESS_KEY_SECRET: ${{ secrets.OSS_ACCESS_KEY_SECRET }}

  produce_node:
    needs: prepare
    if: ${{ needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'node' }}
    uses: ./.github/workflows/_fetch-content-group.yml
    with:
      group: node
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      dev_baseline_sha: ${{ needs.prepare.outputs.dev_baseline_sha }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_SECRET: ${{ secrets.APP_SECRET }}
      SPACE_ID: ${{ secrets.SPACE_ID }}
      FIGMA_API_KEY: ${{ secrets.FIGMA_API_KEY }}
      MODEL_API_KEY: ${{ secrets.MODEL_API_KEY }}
      OSS_ACCESS_KEY_ID: ${{ secrets.OSS_ACCESS_KEY_ID }}
      OSS_ACCESS_KEY_SECRET: ${{ secrets.OSS_ACCESS_KEY_SECRET }}

  produce_go:
    needs: prepare
    if: ${{ needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'go' }}
    uses: ./.github/workflows/_fetch-content-group.yml
    with:
      group: go
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      dev_baseline_sha: ${{ needs.prepare.outputs.dev_baseline_sha }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_SECRET: ${{ secrets.APP_SECRET }}
      SPACE_ID: ${{ secrets.SPACE_ID }}
      FIGMA_API_KEY: ${{ secrets.FIGMA_API_KEY }}
      MODEL_API_KEY: ${{ secrets.MODEL_API_KEY }}
      OSS_ACCESS_KEY_ID: ${{ secrets.OSS_ACCESS_KEY_ID }}
      OSS_ACCESS_KEY_SECRET: ${{ secrets.OSS_ACCESS_KEY_SECRET }}

  produce_cli:
    needs: prepare
    if: ${{ needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'cli' }}
    uses: ./.github/workflows/_fetch-content-group.yml
    with:
      group: cli
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      dev_baseline_sha: ${{ needs.prepare.outputs.dev_baseline_sha }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_SECRET: ${{ secrets.APP_SECRET }}
      SPACE_ID: ${{ secrets.SPACE_ID }}
      FIGMA_API_KEY: ${{ secrets.FIGMA_API_KEY }}
      MODEL_API_KEY: ${{ secrets.MODEL_API_KEY }}
      OSS_ACCESS_KEY_ID: ${{ secrets.OSS_ACCESS_KEY_ID }}
      OSS_ACCESS_KEY_SECRET: ${{ secrets.OSS_ACCESS_KEY_SECRET }}

  produce_rest:
    needs: prepare
    if: ${{ needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'rest' }}
    uses: ./.github/workflows/_fetch-content-group.yml
    with:
      group: rest
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      dev_baseline_sha: ${{ needs.prepare.outputs.dev_baseline_sha }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_SECRET: ${{ secrets.APP_SECRET }}
      SPACE_ID: ${{ secrets.SPACE_ID }}
      FIGMA_API_KEY: ${{ secrets.FIGMA_API_KEY }}
      MODEL_API_KEY: ${{ secrets.MODEL_API_KEY }}
      OSS_ACCESS_KEY_ID: ${{ secrets.OSS_ACCESS_KEY_ID }}
      OSS_ACCESS_KEY_SECRET: ${{ secrets.OSS_ACCESS_KEY_SECRET }}

  publish_guides:
    needs: [prepare, produce_guides, publish_python]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'guides') && needs.produce_guides.outputs.status == 'artifact_ready' }}
    uses: ./.github/workflows/_publish-content-group.yml
    with:
      group: guides
      artifact_name: ${{ needs.produce_guides.outputs.artifact_name || 'unavailable' }}
      commit_message: 'docs(guides): publish fetched content'
      should_publish: ${{ needs.prepare.outputs.publish == 'true' && needs.produce_guides.outputs.status == 'artifact_ready' }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      validate_command: 'node "$GITHUB_WORKSPACE/scripts/validate-generated-sidebars.js"'
  prepare_guides_translation_batches:
    needs: [prepare, publish_guides]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'guides') && (needs.publish_guides.outputs.status == 'published' || needs.publish_guides.outputs.status == 'no_changes') }}
    uses: ./.github/workflows/_prepare-translation-batches.yml
    with:
      group: guides
      source_commit_sha: ${{ needs.publish_guides.outputs.commit_sha || needs.prepare.outputs.dev_baseline_sha }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}

  translate_guides_batches:
    name: guides_translation_batch_${{ matrix.batchNumber }}_of_${{ needs.prepare_guides_translation_batches.outputs.batch_count }}_pending_${{ needs.prepare_guides_translation_batches.outputs.pending_count }}
    needs: [prepare, publish_guides, prepare_guides_translation_batches]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && needs.prepare_guides_translation_batches.result == 'success' && needs.prepare_guides_translation_batches.outputs.batch_count != '0' }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.prepare_guides_translation_batches.outputs.matrix) }}
    uses: ./.github/workflows/_translate-content-group.yml
    with:
      group: guides
      source_commit_sha: ${{ needs.publish_guides.outputs.commit_sha || needs.prepare.outputs.dev_baseline_sha }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      should_translate: true
      max_files: '0'
      batch_index: ${{ matrix.batchIndex }}
      batch_number: ${{ matrix.batchNumber }}
      batch_count: ${{ fromJSON(needs.prepare_guides_translation_batches.outputs.batch_count) }}
      batch_size: ${{ fromJSON(needs.prepare_guides_translation_batches.outputs.batch_size) }}
      pending_count: ${{ fromJSON(needs.prepare_guides_translation_batches.outputs.pending_count) }}
      pending_set_sha256: ${{ needs.prepare_guides_translation_batches.outputs.pending_set_sha256 }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      TRANSLATION_AGENT_API_KEY: ${{ secrets.TRANSLATION_AGENT_API_KEY }}
      REVIEW_AGENT_API_KEY: ${{ secrets.REVIEW_AGENT_API_KEY }}

  publish_guides_translation_batches:
    needs: [prepare, publish_guides, publish_rest, prepare_guides_translation_batches, translate_guides_batches]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && needs.prepare_guides_translation_batches.result == 'success' && needs.prepare_guides_translation_batches.outputs.batch_count != '0' && needs.translate_guides_batches.result == 'success' }}
    uses: ./.github/workflows/_publish-translation-batches.yml
    with:
      group: guides
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      source_commit_sha: ${{ needs.publish_guides.outputs.commit_sha || needs.prepare.outputs.dev_baseline_sha }}
      expected_target_sha: ${{ needs.publish_guides.outputs.commit_sha }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      batch_count: ${{ fromJSON(needs.prepare_guides_translation_batches.outputs.batch_count) }}

  finalize_guides_translation:
    needs: [prepare, prepare_guides_translation_batches, translate_guides_batches, publish_guides_translation_batches]
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      translator_status: ${{ steps.result.outputs.translator_status }}
      publisher_status: ${{ steps.result.outputs.publisher_status }}
      commit_sha: ${{ steps.result.outputs.commit_sha }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: '${{ needs.prepare.outputs.master_sha }}', fetch-depth: 0 }
      - id: result
        env:
          PUBLISH: ${{ needs.prepare.outputs.publish }}
          PREP_RESULT: ${{ needs.prepare_guides_translation_batches.result }}
          BATCH_COUNT: ${{ needs.prepare_guides_translation_batches.result != 'success' && '0' || needs.prepare_guides_translation_batches.outputs.batch_count }}
          BATCH_RESULT: ${{ needs.translate_guides_batches.result }}
          PUBLISHER_RESULT: ${{ needs.publish_guides_translation_batches.result }}
          PUBLISHER_STATUS: ${{ needs.publish_guides_translation_batches.outputs.status }}
          PUBLISHER_COMMIT_SHA: ${{ needs.publish_guides_translation_batches.outputs.commit_sha }}
        run: node scripts/docs-workflow/finalize-translation-batches.js

  publish_python:
    needs: [prepare, produce_python, publish_rest]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'python') && needs.produce_python.outputs.status == 'artifact_ready' }}
    uses: ./.github/workflows/_publish-content-group.yml
    with:
      group: python
      artifact_name: ${{ needs.produce_python.outputs.artifact_name || 'unavailable' }}
      commit_message: 'docs(python): publish SDK reference'
      should_publish: ${{ needs.prepare.outputs.publish == 'true' && needs.produce_python.outputs.status == 'artifact_ready' }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      validate_command: 'node "$GITHUB_WORKSPACE/scripts/validate-generated-sidebars.js"'
  translate_python:
    needs: [prepare, publish_python]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'python') && (needs.publish_python.outputs.status == 'published' || needs.publish_python.outputs.status == 'no_changes') }}
    uses: ./.github/workflows/_translate-content-group.yml
    with:
      group: python
      source_commit_sha: ${{ needs.publish_python.outputs.commit_sha || needs.prepare.outputs.dev_baseline_sha }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      should_translate: ${{ needs.prepare.outputs.publish == 'true' && (needs.publish_python.outputs.status == 'published' || needs.publish_python.outputs.status == 'no_changes') }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      TRANSLATION_AGENT_API_KEY: ${{ secrets.TRANSLATION_AGENT_API_KEY }}
      REVIEW_AGENT_API_KEY: ${{ secrets.REVIEW_AGENT_API_KEY }}
  publish_python_translation:
    needs: [prepare, publish_python, translate_python, publish_guides_translation_batches]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && needs.translate_python.outputs.status == 'translation_ready' }}
    uses: ./.github/workflows/_publish-content-group.yml
    with:
      group: python
      artifact_name: ${{ needs.translate_python.outputs.artifact_name || 'unavailable' }}
      baseline_artifact_name: ${{ needs.translate_python.outputs.baseline_artifact_name || '' }}
      commit_message: 'i18n(python): publish translations'
      should_publish: ${{ needs.prepare.outputs.publish == 'true' && needs.translate_python.outputs.status == 'translation_ready' }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      validate_command: 'node "$GITHUB_WORKSPACE/scripts/validate-generated-sidebars.js" && node "$GITHUB_WORKSPACE/scripts/validate-translated-coverage.js" --group "python"'

  publish_java:
    needs: [prepare, produce_java]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'java') && needs.produce_java.outputs.status == 'artifact_ready' }}
    uses: ./.github/workflows/_publish-content-group.yml
    with:
      group: java
      artifact_name: ${{ needs.produce_java.outputs.artifact_name || 'unavailable' }}
      commit_message: 'docs(java): publish SDK reference'
      should_publish: ${{ needs.prepare.outputs.publish == 'true' && needs.produce_java.outputs.status == 'artifact_ready' }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      validate_command: 'node "$GITHUB_WORKSPACE/scripts/validate-generated-sidebars.js"'
  translate_java:
    needs: [prepare, publish_java]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'java') && (needs.publish_java.outputs.status == 'published' || needs.publish_java.outputs.status == 'no_changes') }}
    uses: ./.github/workflows/_translate-content-group.yml
    with:
      group: java
      source_commit_sha: ${{ needs.publish_java.outputs.commit_sha || needs.prepare.outputs.dev_baseline_sha }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      should_translate: ${{ needs.prepare.outputs.publish == 'true' && (needs.publish_java.outputs.status == 'published' || needs.publish_java.outputs.status == 'no_changes') }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      TRANSLATION_AGENT_API_KEY: ${{ secrets.TRANSLATION_AGENT_API_KEY }}
      REVIEW_AGENT_API_KEY: ${{ secrets.REVIEW_AGENT_API_KEY }}
  publish_java_translation:
    needs: [prepare, publish_java, translate_java, publish_python_translation]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && needs.translate_java.outputs.status == 'translation_ready' }}
    uses: ./.github/workflows/_publish-content-group.yml
    with:
      group: java
      artifact_name: ${{ needs.translate_java.outputs.artifact_name || 'unavailable' }}
      baseline_artifact_name: ${{ needs.translate_java.outputs.baseline_artifact_name || '' }}
      commit_message: 'i18n(java): publish translations'
      should_publish: ${{ needs.prepare.outputs.publish == 'true' && needs.translate_java.outputs.status == 'translation_ready' }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      validate_command: 'node "$GITHUB_WORKSPACE/scripts/validate-generated-sidebars.js" && node "$GITHUB_WORKSPACE/scripts/validate-translated-coverage.js" --group "java"'

  publish_node:
    needs: [prepare, produce_node, publish_java]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'node') && needs.produce_node.outputs.status == 'artifact_ready' }}
    uses: ./.github/workflows/_publish-content-group.yml
    with:
      group: node
      artifact_name: ${{ needs.produce_node.outputs.artifact_name || 'unavailable' }}
      commit_message: 'docs(node): publish SDK reference'
      should_publish: ${{ needs.prepare.outputs.publish == 'true' && needs.produce_node.outputs.status == 'artifact_ready' }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      validate_command: 'node "$GITHUB_WORKSPACE/scripts/validate-generated-sidebars.js"'
  translate_node:
    needs: [prepare, publish_node]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'node') && (needs.publish_node.outputs.status == 'published' || needs.publish_node.outputs.status == 'no_changes') }}
    uses: ./.github/workflows/_translate-content-group.yml
    with:
      group: node
      source_commit_sha: ${{ needs.publish_node.outputs.commit_sha || needs.prepare.outputs.dev_baseline_sha }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      should_translate: ${{ needs.prepare.outputs.publish == 'true' && (needs.publish_node.outputs.status == 'published' || needs.publish_node.outputs.status == 'no_changes') }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      TRANSLATION_AGENT_API_KEY: ${{ secrets.TRANSLATION_AGENT_API_KEY }}
      REVIEW_AGENT_API_KEY: ${{ secrets.REVIEW_AGENT_API_KEY }}
  publish_node_translation:
    needs: [prepare, publish_node, translate_node, publish_java_translation]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && needs.translate_node.outputs.status == 'translation_ready' }}
    uses: ./.github/workflows/_publish-content-group.yml
    with:
      group: node
      artifact_name: ${{ needs.translate_node.outputs.artifact_name || 'unavailable' }}
      baseline_artifact_name: ${{ needs.translate_node.outputs.baseline_artifact_name || '' }}
      commit_message: 'i18n(node): publish translations'
      should_publish: ${{ needs.prepare.outputs.publish == 'true' && needs.translate_node.outputs.status == 'translation_ready' }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      validate_command: 'node "$GITHUB_WORKSPACE/scripts/validate-generated-sidebars.js" && node "$GITHUB_WORKSPACE/scripts/validate-translated-coverage.js" --group "node"'

  publish_go:
    needs: [prepare, produce_go, publish_node]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'go') && needs.produce_go.outputs.status == 'artifact_ready' }}
    uses: ./.github/workflows/_publish-content-group.yml
    with:
      group: go
      artifact_name: ${{ needs.produce_go.outputs.artifact_name || 'unavailable' }}
      commit_message: 'docs(go): publish SDK reference'
      should_publish: ${{ needs.prepare.outputs.publish == 'true' && needs.produce_go.outputs.status == 'artifact_ready' }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      validate_command: 'node "$GITHUB_WORKSPACE/scripts/validate-generated-sidebars.js"'
  translate_go:
    needs: [prepare, publish_go]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'go') && (needs.publish_go.outputs.status == 'published' || needs.publish_go.outputs.status == 'no_changes') }}
    uses: ./.github/workflows/_translate-content-group.yml
    with:
      group: go
      source_commit_sha: ${{ needs.publish_go.outputs.commit_sha || needs.prepare.outputs.dev_baseline_sha }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      should_translate: ${{ needs.prepare.outputs.publish == 'true' && (needs.publish_go.outputs.status == 'published' || needs.publish_go.outputs.status == 'no_changes') }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      TRANSLATION_AGENT_API_KEY: ${{ secrets.TRANSLATION_AGENT_API_KEY }}
      REVIEW_AGENT_API_KEY: ${{ secrets.REVIEW_AGENT_API_KEY }}
  publish_go_translation:
    needs: [prepare, publish_go, translate_go, publish_node_translation]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && needs.translate_go.outputs.status == 'translation_ready' }}
    uses: ./.github/workflows/_publish-content-group.yml
    with:
      group: go
      artifact_name: ${{ needs.translate_go.outputs.artifact_name || 'unavailable' }}
      baseline_artifact_name: ${{ needs.translate_go.outputs.baseline_artifact_name || '' }}
      commit_message: 'i18n(go): publish translations'
      should_publish: ${{ needs.prepare.outputs.publish == 'true' && needs.translate_go.outputs.status == 'translation_ready' }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      validate_command: 'node "$GITHUB_WORKSPACE/scripts/validate-generated-sidebars.js" && node "$GITHUB_WORKSPACE/scripts/validate-translated-coverage.js" --group "go"'

  publish_cli:
    needs: [prepare, produce_cli, publish_go]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'cli') && needs.produce_cli.outputs.status == 'artifact_ready' }}
    uses: ./.github/workflows/_publish-content-group.yml
    with:
      group: cli
      artifact_name: ${{ needs.produce_cli.outputs.artifact_name || 'unavailable' }}
      commit_message: 'docs(cli): publish CLI reference'
      should_publish: ${{ needs.prepare.outputs.publish == 'true' && needs.produce_cli.outputs.status == 'artifact_ready' }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      validate_command: 'node "$GITHUB_WORKSPACE/scripts/validate-generated-sidebars.js"'
  translate_cli:
    needs: [prepare, publish_cli]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'cli') && (needs.publish_cli.outputs.status == 'published' || needs.publish_cli.outputs.status == 'no_changes') }}
    uses: ./.github/workflows/_translate-content-group.yml
    with:
      group: cli
      source_commit_sha: ${{ needs.publish_cli.outputs.commit_sha || needs.prepare.outputs.dev_baseline_sha }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      should_translate: ${{ needs.prepare.outputs.publish == 'true' && (needs.publish_cli.outputs.status == 'published' || needs.publish_cli.outputs.status == 'no_changes') }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      TRANSLATION_AGENT_API_KEY: ${{ secrets.TRANSLATION_AGENT_API_KEY }}
      REVIEW_AGENT_API_KEY: ${{ secrets.REVIEW_AGENT_API_KEY }}
  publish_cli_translation:
    needs: [prepare, publish_cli, translate_cli, publish_go_translation]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && needs.translate_cli.outputs.status == 'translation_ready' }}
    uses: ./.github/workflows/_publish-content-group.yml
    with:
      group: cli
      artifact_name: ${{ needs.translate_cli.outputs.artifact_name || 'unavailable' }}
      baseline_artifact_name: ${{ needs.translate_cli.outputs.baseline_artifact_name || '' }}
      commit_message: 'i18n(cli): publish translations'
      should_publish: ${{ needs.prepare.outputs.publish == 'true' && needs.translate_cli.outputs.status == 'translation_ready' }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      validate_command: 'node "$GITHUB_WORKSPACE/scripts/validate-generated-sidebars.js" && node "$GITHUB_WORKSPACE/scripts/validate-translated-coverage.js" --group "cli"'

  publish_rest:
    needs: [prepare, produce_rest, publish_cli]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'rest') && needs.produce_rest.outputs.status == 'artifact_ready' }}
    uses: ./.github/workflows/_publish-content-group.yml
    with:
      group: rest
      artifact_name: ${{ needs.produce_rest.outputs.artifact_name || 'unavailable' }}
      commit_message: 'docs(rest): publish REST reference'
      should_publish: ${{ needs.prepare.outputs.publish == 'true' && needs.produce_rest.outputs.status == 'artifact_ready' }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      validate_command: 'node "$GITHUB_WORKSPACE/scripts/validate-generated-sidebars.js"'
  translate_rest:
    needs: [prepare, publish_rest]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'rest') && (needs.publish_rest.outputs.status == 'published' || needs.publish_rest.outputs.status == 'no_changes') }}
    uses: ./.github/workflows/_translate-content-group.yml
    with:
      group: rest
      source_commit_sha: ${{ needs.publish_rest.outputs.commit_sha || needs.prepare.outputs.dev_baseline_sha }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      should_translate: ${{ needs.prepare.outputs.publish == 'true' && (needs.publish_rest.outputs.status == 'published' || needs.publish_rest.outputs.status == 'no_changes') }}
      artifact_retention_days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
    secrets:
      TRANSLATION_AGENT_API_KEY: ${{ secrets.TRANSLATION_AGENT_API_KEY }}
      REVIEW_AGENT_API_KEY: ${{ secrets.REVIEW_AGENT_API_KEY }}
  publish_rest_translation:
    needs: [prepare, publish_rest, translate_rest, publish_cli_translation]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && needs.translate_rest.outputs.status == 'translation_ready' }}
    uses: ./.github/workflows/_publish-content-group.yml
    with:
      group: rest
      artifact_name: ${{ needs.translate_rest.outputs.artifact_name || 'unavailable' }}
      baseline_artifact_name: ${{ needs.translate_rest.outputs.baseline_artifact_name || '' }}
      commit_message: 'i18n(rest): publish translations'
      should_publish: ${{ needs.prepare.outputs.publish == 'true' && needs.translate_rest.outputs.status == 'translation_ready' }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      validate_command: 'node "$GITHUB_WORKSPACE/scripts/validate-generated-sidebars.js" && node "$GITHUB_WORKSPACE/scripts/validate-translated-coverage.js" --group "rest"'
  resolve_final:
    needs: [prepare, finalize_guides_translation, publish_python_translation, publish_java_translation, publish_node_translation, publish_go_translation, publish_cli_translation, publish_rest_translation]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      final_dev_sha: ${{ steps.ref.outputs.final_dev_sha }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: master, fetch-depth: 0 }
      - id: ref
        env:
          TARGET_BRANCH: ${{ needs.prepare.outputs.target_branch }}
        run: |
          set -euo pipefail
          git fetch --no-tags origin "refs/heads/$TARGET_BRANCH:refs/remotes/origin/$TARGET_BRANCH"
          sha=$(git rev-parse "refs/remotes/origin/$TARGET_BRANCH")
          [[ "$sha" =~ ^[0-9a-f]{40}$ ]]
          echo "final_dev_sha=$sha" >> "$GITHUB_OUTPUT"

  verify:
    needs: [prepare, resolve_final]
    if: ${{ always() && needs.prepare.outputs.publish == 'true' && needs.resolve_final.outputs.final_dev_sha != '' }}
    uses: ./.github/workflows/_verify-docs.yml
    with:
      final_dev_sha: ${{ needs.resolve_final.outputs.final_dev_sha }}
      master_sha: ${{ needs.prepare.outputs.master_sha }}
      target_branch: ${{ needs.prepare.outputs.target_branch }}
  aggregate:
    needs: [prepare, produce_guides, publish_guides, prepare_guides_translation_batches, translate_guides_batches, publish_guides_translation_batches, finalize_guides_translation, produce_python, publish_python, translate_python, publish_python_translation, produce_java, publish_java, translate_java, publish_java_translation, produce_node, publish_node, translate_node, publish_node_translation, produce_go, publish_go, translate_go, publish_go_translation, produce_cli, publish_cli, translate_cli, publish_cli_translation, produce_rest, publish_rest, translate_rest, publish_rest_translation, resolve_final, verify]
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.master_sha }}
      - uses: pnpm/action-setup@v4
        with: { version: 9 }
      - uses: actions/setup-node@v4
        with: { node-version: '20', cache: npm }
      - run: npm ci
      - id: aggregate
        name: Aggregate terminal results
        env:
          MODE: ${{ needs.prepare.outputs.publish == 'true' && 'publish' || 'artifact_only' }}
          SELECTED_GROUP: ${{ needs.prepare.outputs.selected_group }}
          FINAL_VERIFICATION: ${{ needs.verify.outputs.status || 'failed' }}
          GUIDES_PRODUCER: ${{ needs.produce_guides.outputs.status }}
          GUIDES_SOURCE: ${{ needs.publish_guides.outputs.status }}
          GUIDES_SOURCE_SHA: ${{ needs.publish_guides.outputs.commit_sha }}
          GUIDES_TRANSLATOR: ${{ needs.finalize_guides_translation.outputs.translator_status }}
          GUIDES_TRANSLATION: ${{ needs.finalize_guides_translation.outputs.publisher_status }}
          GUIDES_TRANSLATION_SHA: ${{ needs.finalize_guides_translation.outputs.commit_sha }}
          GUIDES_TRANSLATION_CANDIDATES: ${{ needs.prepare_guides_translation_batches.outputs.candidate_counts }}
          PYTHON_PRODUCER: ${{ needs.produce_python.outputs.status }}
          PYTHON_SOURCE: ${{ needs.publish_python.outputs.status }}
          PYTHON_SOURCE_SHA: ${{ needs.publish_python.outputs.commit_sha }}
          PYTHON_TRANSLATOR: ${{ needs.translate_python.outputs.status }}
          PYTHON_TRANSLATION: ${{ needs.publish_python_translation.outputs.status }}
          PYTHON_TRANSLATION_SHA: ${{ needs.publish_python_translation.outputs.commit_sha }}
          JAVA_PRODUCER: ${{ needs.produce_java.outputs.status }}
          JAVA_SOURCE: ${{ needs.publish_java.outputs.status }}
          JAVA_SOURCE_SHA: ${{ needs.publish_java.outputs.commit_sha }}
          JAVA_TRANSLATOR: ${{ needs.translate_java.outputs.status }}
          JAVA_TRANSLATION: ${{ needs.publish_java_translation.outputs.status }}
          JAVA_TRANSLATION_SHA: ${{ needs.publish_java_translation.outputs.commit_sha }}
          NODE_PRODUCER: ${{ needs.produce_node.outputs.status }}
          NODE_SOURCE: ${{ needs.publish_node.outputs.status }}
          NODE_SOURCE_SHA: ${{ needs.publish_node.outputs.commit_sha }}
          NODE_TRANSLATOR: ${{ needs.translate_node.outputs.status }}
          NODE_TRANSLATION: ${{ needs.publish_node_translation.outputs.status }}
          NODE_TRANSLATION_SHA: ${{ needs.publish_node_translation.outputs.commit_sha }}
          GO_PRODUCER: ${{ needs.produce_go.outputs.status }}
          GO_SOURCE: ${{ needs.publish_go.outputs.status }}
          GO_SOURCE_SHA: ${{ needs.publish_go.outputs.commit_sha }}
          GO_TRANSLATOR: ${{ needs.translate_go.outputs.status }}
          GO_TRANSLATION: ${{ needs.publish_go_translation.outputs.status }}
          GO_TRANSLATION_SHA: ${{ needs.publish_go_translation.outputs.commit_sha }}
          CLI_PRODUCER: ${{ needs.produce_cli.outputs.status }}
          CLI_SOURCE: ${{ needs.publish_cli.outputs.status }}
          CLI_SOURCE_SHA: ${{ needs.publish_cli.outputs.commit_sha }}
          CLI_TRANSLATOR: ${{ needs.translate_cli.outputs.status }}
          CLI_TRANSLATION: ${{ needs.publish_cli_translation.outputs.status }}
          CLI_TRANSLATION_SHA: ${{ needs.publish_cli_translation.outputs.commit_sha }}
          REST_PRODUCER: ${{ needs.produce_rest.outputs.status }}
          REST_SOURCE: ${{ needs.publish_rest.outputs.status }}
          REST_SOURCE_SHA: ${{ needs.publish_rest.outputs.commit_sha }}
          REST_TRANSLATOR: ${{ needs.translate_rest.outputs.status }}
          REST_TRANSLATION: ${{ needs.publish_rest_translation.outputs.status }}
          REST_TRANSLATION_SHA: ${{ needs.publish_rest_translation.outputs.commit_sha }}
        run: |
          node scripts/docs-workflow/build-aggregate-input.js --output tmp/docs-workflow-results.json
          node scripts/docs-workflow/aggregate-results.js --input tmp/docs-workflow-results.json --output tmp/docs-workflow-summary.md
      - name: Restore committed report directories
        if: ${{ always() }}
        continue-on-error: true
        env:
          FINAL_DEV_SHA: ${{ needs.resolve_final.outputs.final_dev_sha }}
        run: |
          set -euo pipefail
          if [[ "$FINAL_DEV_SHA" =~ ^[0-9a-f]{40}$ ]]; then
            git fetch --no-tags origin "$FINAL_DEV_SHA"
            for report_dir in plugins/lark-docs/meta/reports plugins/link-checks/meta/reports; do
              if git cat-file -e "$FINAL_DEV_SHA:$report_dir" 2>/dev/null; then
                rm -rf "$report_dir"
                git checkout "$FINAL_DEV_SHA" -- "$report_dir"
              fi
            done
          fi
      - name: Download current Guides reports
        if: ${{ always() && (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'guides') }}
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: docs-checkpoint-guides-${{ github.run_id }}-reports
          path: plugins/lark-docs/meta/reports
      - name: Download Guides translation publication report
        if: ${{ always() && needs.prepare_guides_translation_batches.outputs.batch_count != '' && needs.prepare_guides_translation_batches.outputs.batch_count != '0' }}
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: docs-translation-publication-guides-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/guides-translation-publication-evidence
      - id: reports
        name: Collect card report summaries
        if: ${{ always() }}
        continue-on-error: true
        env:
          CARD_REPORT_STARTED_AT: ${{ needs.prepare.outputs.card_started_at }}
          CARD_REPORT_REF: ${{ needs.resolve_final.outputs.final_dev_sha }}
          CARD_REPORT_ARTIFACT_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}#artifacts
          CARD_EXPECT_GUIDES_REPORTS: ${{ (needs.prepare.outputs.selected_group == 'all' || needs.prepare.outputs.selected_group == 'guides') && needs.produce_guides.outputs.status == 'artifact_ready' }}
          CARD_BASE_NOTES_JSON: ${{ steps.aggregate.outputs.notes_json }}
          CARD_EXPECT_GUIDES_PUBLICATION_REPORT: ${{ needs.prepare_guides_translation_batches.outputs.batch_count != '' && needs.prepare_guides_translation_batches.outputs.batch_count != '0' }}
          CARD_GUIDES_PUBLICATION_REPORT: ${{ runner.temp }}/guides-translation-publication-evidence/publication-report.json
          CARD_GUIDES_RUN_ID: ${{ github.run_id }}
          CARD_GUIDES_RUN_ATTEMPT: ${{ github.run_attempt }}
          CARD_GUIDES_MASTER_SHA: ${{ needs.prepare.outputs.master_sha }}
          CARD_GUIDES_SOURCE_SHA: ${{ needs.publish_guides.outputs.commit_sha }}
          CARD_GUIDES_TARGET_SHA: ${{ needs.publish_guides.outputs.commit_sha }}
          CARD_GUIDES_STAGING_SHA: ${{ needs.publish_guides_translation_batches.outputs.staging_sha }}
          CARD_GUIDES_PUBLISHER_RESULT: ${{ needs.publish_guides_translation_batches.result }}
          CARD_GUIDES_PENDING_SET_SHA256: ${{ needs.prepare_guides_translation_batches.outputs.pending_set_sha256 }}
          CARD_GUIDES_FINAL_TRANSLATOR_STATUS: ${{ needs.finalize_guides_translation.outputs.translator_status }}
          CARD_GUIDES_FINAL_PUBLISHER_STATUS: ${{ needs.finalize_guides_translation.outputs.publisher_status }}
          CARD_GUIDES_FINAL_COMMIT_SHA: ${{ needs.finalize_guides_translation.outputs.commit_sha }}
        run: node scripts/collect-build-card-notes.js
      - name: Create final card report artifact
        if: ${{ always() }}
        continue-on-error: true
        run: |
          set -euo pipefail
          reports_file="${{ steps.reports.outputs.card_notes_file }}"
          if [[ -z "$reports_file" || ! -f "$reports_file" ]]; then
            reports_file="$RUNNER_TEMP/card-notes.json"
            printf '[]\n' > "$reports_file"
          fi
          printf '%s\n' "${CARD_SUMMARY:-Documentation workflow report unavailable.}" > "$RUNNER_TEMP/card-summary.txt"
          node scripts/docs-workflow/docs-card-report.js create \
            --run-id "$GITHUB_RUN_ID" \
            --overall-status "$OVERALL_STATUS" \
            --summary-file "$RUNNER_TEMP/card-summary.txt" \
            --reports-json "$reports_file" \
            --output tmp/docs-card-report/card-report.json
        env:
          OVERALL_STATUS: ${{ steps.aggregate.outputs.overall_status == 'success' && 'success' || 'failure' }}
          CARD_SUMMARY: ${{ steps.aggregate.outputs.summary_text }}
      - name: Upload final card report artifact
        if: ${{ always() }}
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: docs-card-report-${{ github.run_id }}
          path: tmp/docs-card-report/card-report.json
          if-no-files-found: warn
          retention-days: ${{ fromJSON(needs.prepare.outputs.retention_days) }}
      - name: Fail unsuccessful workflow
        if: ${{ steps.aggregate.outputs.overall_status != 'success' }}
        run: exit 1

  finalize_card_fallback:
    needs: [prepare, aggregate, monitor_docs_progress]
    if: ${{ always() && needs.prepare.outputs.card_id != '' && needs.monitor_docs_progress.result != 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@v4
        with: { ref: '${{ needs.prepare.outputs.master_sha }}' }
      - uses: pnpm/action-setup@v4
        with: { version: 9 }
      - uses: actions/setup-node@v4
        with: { node-version: '20', cache: npm }
      - run: npm ci
      - uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: docs-card-report-${{ github.run_id }}
          path: tmp/docs-card-report
      - name: Perform best-effort terminal card update
        continue-on-error: true
        run: node scripts/docs-workflow/monitor-docs-progress.js --finalize-only --report-file tmp/docs-card-report/card-report.json
        env:
          GITHUB_TOKEN: ${{ github.token }}
          CARD_ID: ${{ needs.prepare.outputs.card_id }}
          CARD_STARTED_AT: ${{ needs.prepare.outputs.card_started_at }}
          CARD_TARGET_BRANCH: ${{ needs.prepare.outputs.target_branch }}
          SELECTED_GROUP: ${{ needs.prepare.outputs.selected_group }}
          PUBLISH_ENABLED: ${{ needs.prepare.outputs.publish }}
          APP_ID: ${{ secrets.APP_ID }}
          APP_SECRET: ${{ secrets.APP_SECRET }}
          FEISHU_HOST: ${{ vars.FEISHU_HOST }}
